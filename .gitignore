# deps
node_modules/

# rust / soroban build artifacts
contracts/otc/target/
contracts/**/target/
contracts/**/test_snapshots/

# circuit build artifacts (large; regenerated by scripts). Keep final keys + vks.
circuits/build/*
!circuits/build/.gitkeep
!circuits/build/*_final.zkey
!circuits/build/*_vk.json

# verifier WASMs are large; rebuilt from VKs
contracts/build/*.wasm

# reference repo (study-only, points to V1's _reference; never committed)
_reference/

# misc
tools/bin/ffmpeg.exe
*.log
.DS_Store
.vercel/
scratchpad/

# platform binaries (download separately)
tools/bin/

.vercel

.vercel/
